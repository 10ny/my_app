<% provide(:title, "ユーザー一覧") %>
<div class="container mypage row">
  <h1 class="center">ユーザー一覧</h1>
  <ul class="users">
    <%= render @users %>
  </ul>
  <div>
    <%#= page_entries_info @users %>
    <%= paginate @users %>
  </div>
</div>