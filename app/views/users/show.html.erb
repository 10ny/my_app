<% provide(:title, "マイページ") %>
<div class="container mypage row">
  <div class="container">
    <div class="row">
      <div class="col-3">
        <h2>image</h2> <%# 画像はここに挿入 %>
      </div>
      <div class="col-9">
        <h2><%= @user.nickname %></h2>
      </div>
    </div>
  </div>
  <div class="conrtainer">
    <%= @user.name %>
  </div>
  <%= link_to "アカウント編集", edit_user_path(current_user), class: "btn btn-info col-6" %>
  <%= link_to "アカウント削除", edit_user_path(current_user), class: "btn btn-danger col-6" %> <%# リンクは変える %>
</div>